<div class="h-flex">
  <div class="c-grid--medium-60">
    {{ item.media[item.header.featured_image[0]].html() }}
  </div>
  <div class="c-grid--medium-40 h-flex h-flex--vertical">
    <div>{{ item.media[item.header.featured_image[1]].html() }}</div>
    <div class="h-hide-for-mobile" style="padding-right: 50%">{{ item.media[item.header.featured_image[2]].html() }}</div>
  </div>
</div>
