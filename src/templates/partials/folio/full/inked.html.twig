<div class="c-grid">
    <div class="c-grid--medium-50"></div>
    <div class="c-grid--medium-50" style="margin-bottom: 1rem">{{ item.media[item.header.featured_image[4]].html() }}</div>
</div>
<div class="c-grid c-grid--flex-start">
  <div class="c-grid--medium-70">{{ item.media[item.header.featured_image[0]].html() }}</div>
  <div class="c-grid--medium-24">{{ item.media[item.header.featured_image[1]].html() }}</div>
  <div class="c-grid--medium-38">{{ item.media[item.header.featured_image[2]].html() }}</div>
  <div class="c-grid--medium-60">{{ item.media[item.header.featured_image[3]].html() }}</div>
</div>
